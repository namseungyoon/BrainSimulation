# Notion 정정 대기 목록 (커넥터 재연결 후 적용)

대상 페이지: **카테고리 정리판** `3b317cb5cdbd8147935aff4628367d2f`
사유: MEA 실험(E12)의 **방출 모드 오보**(확률 → 실제는 결정론) + 규모 명시 누락.
E1~E3·E8의 "확률 방출" 표기는 **정확하므로 손대지 않는다**(sc_full_slice.py는 기본 확률).

---

## 1) Part 2.3 계산 환경 콜아웃 ④

**찾기**
```
MEA 실험(E12)은 생리적 현실성을 위해 **확률 방출**을 사용했다.
```
**바꾸기**
```
★**정정(2026-08-05)**: MEA 실험(E12)은 실제로 **결정론(det=True)** 으로 실행되었다. 코드 기본값이 결정론이었고 실행 시 `--prob`을 주지 않았다(저장 npz 전수 확인). 이전에 '확률 방출'이라 적은 것은 오보이다.
```

## 2) Part 5.1.1 실험 환경 표 — 시냅스 방출 행

**찾기**
```
<td>**확률 방출**(BBP EMS, Random123) — 생리적 현실성</td>
```
**바꾸기**
```
<td>**결정론(det=True, 룰베이스)** — 단기가소성(Use/Dep/Fac)은 적용되고 방출만 확률이 아니다. ⚠정정: 이전 '확률 방출' 표기는 오보</td>
```

## 3) Part 5.1.4 시냅스 개수 표 — 내부 커넥톰 행

**찾기**
```
<td>Ecker(2020) Table 3 9클래스 확률 EMS(단기가소성 포함)</td>
```
**바꾸기**
```
<td>Ecker(2020) Table 3 9클래스 · **결정론 EMS**(DetAMPANMDA/DetGABAAB, 단기가소성 포함) · 전체 슬라이스 6,438,379 연결 중 서브셋에 속한 7.2%</td>
```

## 4) Part 5.4 결과 E 제목·도입문

**찾기**
```
### 5.4 결과 E — 재현성 (확률 방출 시드 변경) {color="green"}
확률 방출·시냅스 배치·섬유 배정 시드를 바꾼 동일 프로토콜 2회(각 80분):
```
**바꾸기**
```
### 5.4 결과 E — 재현성 (시냅스 배치·섬유 배정 시드 변경) {color="green"}
**시냅스 배치·섬유 배정·내부 커넥톰 배치** 시드를 바꾼 동일 프로토콜 2회(각 80분). ⚠정정: 방출은 **결정론**이므로 이 변동은 확률 방출 때문이 아니다 — 시드가 (a) 내부 커넥톰 배치 (b) SC 시냅스 위치 (c) 섬유 배정을 바꿔서 생긴다:
```

## 5) 추가할 규모 콜아웃 (Part 5.1 도입부 또는 5.1.3 아래)

```
<callout icon="📏" color="blue_bg">
	**규모 명시(필수)** — 이 Part의 모든 결과는 **2,000세포 서브셋**(전체 17,647의 11.3%)이다. 내부 시냅스 466,440(전체 6,438,379의 7.2%) · SC 시냅스 6,081(자극전극 층대 200µm 내 수상돌기만). 전규모 1회가 12~43시간이라 서브셋을 사용했고, **서브셋 결과를 전규모로 외삽하지 않는다**(규모 보정 실험 예정).
</callout>
```

---

적용 후 이 파일은 삭제하거나 "적용 완료" 표시를 남긴다.
