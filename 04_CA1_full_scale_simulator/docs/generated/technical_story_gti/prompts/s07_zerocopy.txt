Create one complete 16:9 academic scientific PowerPoint slide image, not a background. Dense but readable computational-neuroscience/HPC slide. White background, black crisp text, thin grid lines, muted blue/red/green accents, small monospace for numbers. Every slide MUST have three clearly labeled horizontal bands: "판단 근거 (WHY)", "방법 (HOW)", "결과 (RESULT)". Keep all text legible.
TITLE: Act 1.6 -- Connect marshalling: measured 4.85x, and a rejected idea
Band WHY: the explicit-edge GPU connect was slow. Hypothesis: the bottleneck is host-side marshalling (NumPy -> Python list -> ctypes), not the GPU work. Constraint: single GPU, no MPI; connections must stay bit-identical.
Band HOW: (1) zero-copy marshalling -- pass contiguous np.uint32 data pointers instead of Python lists/ctypes (opt-in, no rebuild). (2) a fused explicit-array C API filling source/target/weight/delay/port/syngroup in ONE CUDA kernel (rebuild). Benchmarked all three arms on GPU1, same artifact/seed.
Band RESULT: build+connect 338.3 s (chunked) -> 69.8 s zero-copy (4.85x) -> 68.7 s fused (+1.6%, noise). Spikes bit-identical (3780). Verdict: marshalling WAS the bottleneck; zero-copy is the permanent default; the fused kernel's 80% launch cut does NOT move wall-time -> kept opt-in only. Show the 3-bar benchmark.
