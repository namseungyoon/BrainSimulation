Create one complete 16:9 academic scientific PowerPoint slide image, not a background. Dense but readable computational-neuroscience/HPC slide. White background, black crisp text, thin grid lines, muted blue/red/green accents, small monospace for numbers. Every slide MUST have three clearly labeled horizontal bands: "판단 근거 (WHY)", "방법 (HOW)", "결과 (RESULT)". Keep all text legible.
TITLE: Act 2.3 -- Honest measurement: band power is not oscillation
Band WHY: the original harness PASSED theta on 1/f noise (band-edge argmax, no prominence). So earlier "theta" was never trustworthy. Judgment: fix the instrument before trusting any result.
Band HOW: rebuilt the gates -- Welch PSD; theta requires peak PROMINENCE >= 3x a 1/f aperiodic fit (reject band-edge); gamma within 71+-20 Hz; surrogate-significant CFC (199 perms); per-type modulation-depth gate; phase only with a valid theta peak + >=8 cycles. Also fixed the LFP proxy (128 spatially-sampled pyramidal; distal current must use V_dist not V_d).
Band RESULT: the baseline full-scale result now correctly FAILS theta/gamma (was a false pass). The instrument is honest; every later claim rests on it. Draw a true peak vs a 1/f slope with a 3x prominence bracket.
